/** @type {import('postcss-load-config').Config} */
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {}, // Pode ser adicionado para garantir compatibilidade com browsers
  },
};
